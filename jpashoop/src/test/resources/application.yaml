# spring:
  # datasource:
  #   url: jdbc:h2:mem:test
  #   username: sa
  #   password:
  #   driver-class-name: org.h2.Driver

  # jpa:
  #   hibernate:
  #     ddl-auto: create
  #   properties:
  #     hibernate:
  #       # System.out을 통해 로그를 남김
  #       # show_sql: true
  #       format_sql: true

logging:
  level:
    # Logger를 통해 log를 남기는 설정
    org.hibernate.SQL: debug
    # 쿼리 파라미터 출력
    org.hibernate.orm.jdbc.bind: trace  # Spring Boot 3.x에서 변경된 부분